# Fonts

Bundled:

- `montserrat/static/Montserrat-Medium.ttf`
- `montserrat/static/Montserrat-SemiBold.ttf`
  (SIL Open Font License — see `montserrat/OFL.txt`)

Icons are **not** bundled (licensing). Drop any icon font into `icons/`
and point `icon_font` at it. See `icons/README.md`.
